apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: https://prometheus:9090/prometheus
    isDefault: true
    editable: true
    jsonData:
      tlsSkipVerify: true
